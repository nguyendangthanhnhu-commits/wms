export default function WarehouseDetailPage({ params }: { params: Promise<{ id: string }> }) {
  return (
    <div className="text-sm text-muted-foreground">
      Chi tiết kho — TODO (id sẽ được resolve async trong Next.js)
    </div>
  );
}
